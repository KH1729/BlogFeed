# Plan: new-post-dialog

1. Refactor `NewPostForm` (remove card; optional `onCancel`, `showHeading`).
2. Add `NewPostDialog` with MUI `Dialog`.
3. Update `App` layout: header button + dialog; adjust empty state copy.
4. Add tests for `NewPostDialog`; run Vitest + production build.
